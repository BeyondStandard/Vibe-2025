# Leave empty to use Vite dev server proxy (see vite.config.ts) — requests go to /api → FastAPI :8000
# VITE_API_BASE_URL=

# Demo login (optional overrides; defaults: demo@example.com / demo)
# VITE_DEMO_EMAIL=demo@example.com
# VITE_DEMO_PASSWORD=demo
